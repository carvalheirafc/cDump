#include <stdio.h>
#include <stdlib.h>
#include "memory-partition.h"

int main(int argc, char **argv){
	
	int size;
	scanf("%d", &size);
	struct Memomry memory_array[size];
	memory_list.total_size = size;
	init(&memo)

	return(EXIT_SUCCESS);
}
